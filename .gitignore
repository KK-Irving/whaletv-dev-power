示例/
node_modules/
dist/
*.tsbuildinfo

# 编译/运行时临时产物
*.tmp
tsc-out.txt
*.out.txt

# 测试脚本（含临时凭据，不进 git）
scripts/test-*.mjs
scripts/test-*.cmd

# 临时数据目录：采样脚本输出、模块地图原始数据、含凭据的临时文件
.scratch/

# Issue 工作目录：prepare_issue_workspace 工具自动创建，含从 Zmind 下载的附件
# 不进 git，避免污染源码仓库且可能含 PII / 客户敏感信息
.workspace/

# 本地知识库数据库：knowledge-mcp-server 运行时自动创建，按环境不同内容不同
data/
